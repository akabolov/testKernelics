export enum EnvVariables {
  repoList = 'GITHUB_REPOSITORIES',
  concurrencyLimit = 'CONCURRENCY_LIMIT',
  githubUsername = 'GITHUB_USERNAME',
  githubToken = 'GITHUB_TOKEN',
}

export enum ContentType {
  file = 'file',
  dir = 'dir',
}
